This folder is reserved for future feature modules consolidated under `features/`.
Current code is organized by feature already under `app/` (e.g., dashboard/, signin/).
As you add new capabilities, prefer creating `features/<name>/` folders following the same pattern.
