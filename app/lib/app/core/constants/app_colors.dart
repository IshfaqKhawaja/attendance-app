import 'package:flutter/material.dart';

/// App brand colors centralized here for reuse across themes and widgets.
class AppColors {
  AppColors._();

  // Brand palette
  static const Color primary = Color(0xFF012B4A);
  static const Color secondary = Color(0xFF425C8C);
  static const Color tertiary = Color(0xFFEEF3F6);

  // Semantic colors (extend as needed)
  static const Color success = Color(0xFF2E7D32);
  static const Color warning = Color(0xFFF9A825);
  static const Color error = Color(0xFFC62828);
}
